package org.example.core;

/**
 * Purpose:
 * Should manage the creation, retrieval, and cleanup of Scene objects.
 * It should act as a factory and lifecycle manager for scenes.
 *
 * Key Actions Needed:
 * startNewGame(Player player): Create and initialize the first scene for a new game.
 *
 * getCurrentScene(): Provide access to the active scene.
 *
 * cleanup(): Clear the current scene to free resources.
 *
 * Design Principle:
 * Single Responsibility Principle (SRP): Its only job is to
 * manage scenes, isolating this logic from the GameManager.
 */

//TODO: Impleement methods and design principle
public class SceneManager {
}
